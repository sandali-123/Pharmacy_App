<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.example.pharmacyapp">

    <application
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:textSize="20dp"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/AppTheme">

        <activity android:name=".newPrescriptions" android:label="New Prescriptions"  android:parentActivityName=".AdminPanel">
            <meta-data
                android:name="android.support.PARENT_ACTIVITY"
                android:value=".AdminPanel" />> </activity>
        <activity android:name=".ViewPrescriptions" android:label="New Prescriptions" android:parentActivityName=".AdminPanel">
        <meta-data
            android:name="android.support.PARENT_ACTIVITY"
            android:value=".AdminPanel" />> </activity>
        <activity android:name=".NewOrederitems" android:label="Order Items" android:parentActivityName=".AdminPanel">
            <meta-data
                android:name="android.support.PARENT_ACTIVITY"
                android:value=".AdminPanel" />> </activity>
        <activity android:name=".admin_newOrders" android:label="New Orders" android:parentActivityName=".AdminPanel">
            <meta-data
                android:name="android.support.PARENT_ACTIVITY"
                android:value=".AdminPanel" />> </activity>
        <activity android:name=".editItems" android:label="Manage Items" android:parentActivityName=".AdminPanel">
            <meta-data
                android:name="android.support.PARENT_ACTIVITY"
                android:value=".AdminPanel" />> </activity>
        <activity android:name=".ManageProducts" android:parentActivityName=".AdminPanel">
            <meta-data
                android:name="android.support.PARENT_ACTIVITY"
                android:value=".AdminPanel" />> </activity>
        <activity android:name=".AddCategory" android:parentActivityName=".AdminPanel">
            <meta-data
                android:name="android.support.PARENT_ACTIVITY"
                android:value=".AdminPanel" />>
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
        <activity android:name=".AdminPanel">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
        <activity android:name=".AddItem" android:parentActivityName=".AdminPanel">
            <meta-data
                android:name="android.support.PARENT_ACTIVITY"
                android:value=".AdminPanel" />> </activity>
        <activity android:name=".MainActivity" />
    </application>

</manifest>